# 第1章 拡張性――HOSTS.TXTと分散管理のケーススタディ

> この章は本文ではなく構成案である。HOSTS.TXTの通史ではなく、拡張性と分散管理の設計判断を検討する。歴史の因果関係は、一次資料の記述範囲を確認してから文章化する。

## 1. 要件の定義と評価観点

ここでの拡張性は、ホストやデータの件数だけでなく、更新頻度、更新主体、問い合わせ元、管理組織が増えても、中央の処理量や調整待ちを無制限に増やさず運用できる性質とする。

- 登録や変更を処理する中央主体の作業量と待ち時間。
- 一覧のサイズ、配布回数、各取得者が必要とする帯域と処理量。
- 各組織が自らの範囲を変更できる度合いと、全体で一意性を保つための調整量。
- 分割後に必要となる委譲、参照、障害調査の複雑さ。

## 2. この章の問い

単一のホスト表は、小規模なネットワークでは何を簡単にし、規模の拡大とともに何を難しくしたのか。データ量だけでなく更新主体の増加は、なぜ中央の調整を難しくするのか。管理責任を分散するには、データの分割、一意性、参照方法、責任境界をどう設計する必要があるのか。後知恵の「集中管理は悪い」という結論にせず、要件と条件から検討する。

## 3. 問題が現れた歴史的背景

- HOSTS.TXT以前に、人と機械がホストをどのように識別・参照したか。
  - TODO: 要出典――時期とネットワークを限定し、数値アドレス、ローカルな名称対応、初期ホスト表を区別する。
- ARPANETの成長と、参加ホスト・管理主体の変化。
  - RFC 1034の導入部が回顧する問題を起点とし、同時代資料で補強する。
- 「DNS以前」を単一期間にまとめず、HOSTS.TXT導入前、集中配布が機能した時期、限界が顕在化した時期に分ける。

## 4. 従来方式――HOSTS.TXTの仕組み

- 一つのホスト表へ、ホスト名とアドレスなどを登録し、各ホストが取得して利用する流れ。
- 登録、マスターの維持、配布、ローカルでの参照を別の工程として図示する。
- どの組織がどの時期に各工程を担ったかは、資料確認後に限定して記す。
- HOSTS.TXTの書式と運用の変遷を同一視しない。

## 5. 規模拡大によって起きた問題

### 集中管理の限界

- 変更要求が中央の調整点へ集まることによる処理量と調整コスト。
- 中央の一覧を正として扱う簡潔さと、管理主体が増えたときの自律性の不足。
- 集中管理、集中保管、集中配布を分け、どれがどの問題を生んだかを記す。

### 更新頻度と配布負荷

- 表全体の取得と、変更件数・取得主体の増加との関係。
- 更新から各利用者の取得までに時間差があるため、同時点でも参照内容が異なり得ること。
- TODO: 要検証――転送量、更新頻度、ホスト数の具体値は、時点と計測方法を確認するまで掲載しない。

### 更新主体の増加

- データが同じ大きさでも、独立した組織からの登録・変更要求が増えると、検証、名前の調整、受付順序、責任確認が中央へ集中する。
- 転送帯域の問題と、管理権限・組織間調整の問題を混同しない。
- TODO: 要出典――当時の登録工程と負荷を、運用主体と時期を限定して確認する。

### 名前衝突

- 平坦な名前空間で一意性を中央が調整する負担。
- 組織が独立に名前を選ぶ要求と、ネットワーク全体で衝突を避ける要求の緊張。
- TODO: 要出典――衝突の実例や頻度を、後世の要約だけで断定しない。

### 可用性と単一障害点

- マスターの保守・配布サービスが利用できない場合、新規取得や更新がどう影響を受けるか。
- 取得済みのローカルコピーによって既知の名前を参照できる点を併記する。「中央停止ですべての名前解決が即時停止した」とは書かない。
- 管理上の単一障害点と、問い合わせ時の単一障害点を区別する。

## 6. DNSに求められた要件

| 問題 | 要件としての読み替え | 後続章で検討する設計 |
| --- | --- | --- |
| 中央への登録・調整集中 | 拡張性、運用性、管理の自律性 | 階層的名前空間、委譲、ゾーン |
| 一覧全体の反復配布 | 配布効率、性能 | 分散した権威情報、問い合わせ、キャッシュ |
| 変更の反映遅延 | 鮮度と負荷の制御 | TTL、ゾーン転送 |
| 平坦な名前の衝突 | 一意性と分散管理の両立 | 階層名と委譲 |
| 中央機能への依存 | 可用性、障害耐性 | 複数の権威サーバー、複製 |
| 既存利用からの移行 | 後方互換性、段階的移行 | 移行期の仕組み（TODO: 要検証） |

この表は本書による分析枠組みであり、当時の文書が同じ非機能要件名を列挙したという主張ではない。

## 7. DNSの設計――階層化、ゾーン、委譲

- 階層的な名前空間によって、名前の一意性を部分木ごとの責任へ分解する考え方を説明する。
- ドメインを名前空間上の部分木、ゾーンを一つの管理主体が権威を持つ範囲として区別する。
- 委譲によって、親が全データを管理せず、子の管理主体と到達方法を示す設計を扱う。
- 分散した権威情報をリゾルバがたどることで、一括配布から必要な情報を問い合わせる方式へ責任を移す。
- 上記の説明はRFC 1034/1035と現行用語を照合し、初期仕様と現在の用語差を明記する。

## 8. 効果、制約、他要件との衝突

### 得られる効果

- 登録・更新作業とデータ保持を管理範囲ごとに分け、中央が個々の変更を処理する必要を減らす。
- 組織が委譲された範囲を自律的に管理しながら、階層全体で名前を参照できる。
- 問い合わせとキャッシュにより、利用者全員へ一覧全体を繰り返し配る必要を減らす。

### 新しく生じる制約

- 親子の委譲情報、ゾーン境界、権威サーバーへの到達性という新しい設定と依存関係が生じる。
- 名前空間の階層と組織・運用上の境界が常に一致するとは限らず、ドメインとゾーンを区別する必要がある。
- 問い合わせ経路が多段になり、障害箇所と責任主体の切り分けが複雑になる。

### 他の非機能要件との衝突

- 分散は拡張性と自律性を助ける一方、整合性確認と運用の観測点を増やす。
- 委譲先の障害や誤設定は、その範囲の可用性へ影響する。分散だけで障害耐性が保証されるわけではない。
- キャッシュは配布負荷を減らす一方、変更反映を遅らせる。この詳細は性能と整合性の章へ委ねる。

## 9. 運用上の注意と他システムへの応用

- 管理責任を分ける際は、データの分割だけでなく、権限、参照方法、障害時の連絡先、境界をまたぐ整合確認を設計する。
- 階層化が適するのは、全体の一意性を保ちながら部分ごとに安定した責任主体を置ける場合である。変更が常に複数境界をまたぐシステムでは調整コストが残る。
- 分割後の局所最適だけでなく、参照経路の長さ、共通依存先、誤設定の影響範囲を評価する。

## 10. 章の流れ

1. 小規模では、共有された表が理解しやすく有効だったことを示す。
2. 登録、維持、配布、参照のデータフローを示す。
3. 規模と管理主体の増加が各工程へ与えた負荷を分けて検討する。
4. 問題を拡張性と分散管理の要件へ読み替える。
5. 階層的名前空間、ゾーン、委譲が責任とデータをどう分割するかを示す。
6. 分散管理が増やす依存関係と、他要件との衝突を評価する。

## 11. 参照すべき一次資料

- [RFC 1034, 2章](https://www.rfc-editor.org/rfc/rfc1034.html#section-2): HOSTS.TXT方式の問題とDNSの設計目標を裏付ける中心資料。
- [RFC 1035](https://www.rfc-editor.org/rfc/rfc1035.html): DNSの実装・プロトコル側の初期仕様。後続設計との対照に使う。
- [RFC 810](https://www.rfc-editor.org/rfc/rfc810.html): Internet Name Server Protocol。DNS以前からDNSへの移行期の文脈を確認する候補。
- [RFC 811](https://www.rfc-editor.org/rfc/rfc811.html): HOSTS.TXTの当時の仕様・運用を確認する候補。
- [RFC 882](https://www.rfc-editor.org/rfc/rfc882.html) / [RFC 883](https://www.rfc-editor.org/rfc/rfc883.html): RFC 1034/1035以前のドメイン名設計を比較する候補。

RFC 810、811、882、883から具体的事実を本文へ採用する前に、本文と後続RFCによる更新関係を確認する。

## 12. 本文で断定を避けるべき点

- DNSが「一人」または「一つの問題」だけによって発明されたという単純化。
- HOSTS.TXTの中央機能が停止すると、取得済み情報を含む全名前解決が直ちに止まったという表現。
- ホスト数、ファイルサイズ、転送回数、衝突件数の、出典と時点を欠く数値。
- 階層化、委譲、キャッシュのどれか一つが、すべての拡張性問題を解決したという評価。
- 後世の用語や現在の役割分担が、移行期にも同じだったという前提。
- RFC 1034の回顧だけを、1980年代初頭の全運用実態を直接観測した記録として扱うこと。
