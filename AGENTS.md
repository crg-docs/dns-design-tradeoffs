# AIエージェント向け執筆規約

この規約はリポジトリ全体に適用する。

## 正確性と出典

- 事実、資料から導ける解釈、筆者の推測を明確に分離する。
- 歴史的事実には、原則として一次資料の出典を付ける。
- RFCの規定を適用範囲や前提から切り離して一般化しすぎない。
- 現在の仕様と歴史上の仕様を混同しない。廃止、更新、位置付けの変更も確認する。
- 用語は RFC 9499 などの現行用語集を基準とし、歴史資料で用法が異なる場合は併記する。
- 不確かな情報を断定しない。未確認箇所には `TODO: 要出典` または `TODO: 要検証` を付ける。
- 存在を確認していない文献、URL、引用文を作らない。

## 編集

- Markdownで執筆する。
- 既存文章の意図と論旨を確認し、合意なく変更しない。
- 一度の変更で複数章の本文を大量生成しない。構成、出典、短い節という単位に分ける。
- 機能の列挙ではなく、問題、要件、設計、効果、代償の関係を示す。
- 章構成は非機能要件を主軸とし、歴史は設計が必要になった背景として必要な範囲で扱う。
- 「高可用」「高速」などの定性的な語だけで済ませず、対象、範囲、時間、負荷、失敗時の振る舞いを具体化する。
- 変更後は `git diff --check` と `git diff` で差分を確認する。
- 文体、表記、引用方法は `docs/style-guide.md` に従う。
